# Local data governance

`data/raw/` is intentionally ignored. Never commit, upload, or redistribute exchange-derived data. The repository contains only deterministic synthetic fixtures generated by `python -m eventbook.synthetic.generate`.
